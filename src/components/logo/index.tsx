import { Box, Typography } from "@mui/material";

export const Logo = () => {
  return (
    <Box display="flex">
      <Typography color="secondary" variant="h6">
        ENGAGE LOGO
      </Typography>
    </Box>
  );
};
